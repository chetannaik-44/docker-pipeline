trigger:
  branches:
    include:
      - develop

variables:
  - group: default_credentails
  - template: templates/vars/vars.yml

stages:
  - template: templates/stages/build_stage.yml
    parameters:
      pipeline_type: develop